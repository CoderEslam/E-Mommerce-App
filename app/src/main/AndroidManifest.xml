<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.doubleclick.e_commerceapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ThemeApp">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".DeliveryDrivers.SettingDriverActivity" />
        <activity android:name=".Activites.Admin.Control.LoginDriverActivity" />
        <activity android:name=".Activites.Admin.Control.LoginSellerActivity" />
        <activity
            android:name=".DeliveryDrivers.DriverMapActivity"
            android:label="@string/title_activity_driver_map" />
        <activity
            android:name=".DeliveryDrivers.SellerMapActivity"
            android:label="@string/title_activity_seller_map" />
        <activity android:name=".Advertisement.AdvertisementActivity" />
        <activity android:name=".Activites.User.SeeAllCategory.SeeAllCatrgoryActivity" />
        <activity android:name=".Activites.Admin.Control.EditOnAdminActivity" />
        <activity android:name=".Activites.Admin.Control.AddCategoryActivity" />
        <activity android:name=".Activites.Admin.ViewProductItem" />
        <activity android:name=".Activites.LoginActivity" />
        <activity android:name=".Activites.RegisterActivity" />
        <activity android:name=".Activites.Admin.AdminCategoryActivity" />
        <activity android:name=".Activites.Admin.AdminAddNewProductActivity" />
        <activity
            android:name=".Activites.User.HomeActivity"
            android:hardwareAccelerated="false" />
        <activity android:name=".Activites.User.SettingsDataBase.SettinsActivity" />
        <activity android:name=".Activites.User.ProductDetails.ProductDetailsActivity" />
        <activity android:name=".Activites.User.CartActivity" />
        <activity android:name=".Activites.User.ConfirmFinalOrderActivity" />
        <activity android:name=".Activites.Admin.AdminNewOrdersActivity" />
        <activity android:name=".Activites.Admin.AdminUserProductsActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Activites.Login_SignIn.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>